@model IEnumerable<CategoryViewModel>

@{ 
    ViewBag.Title = "All categories";
    Layout = "_AdminLayout";
}

<table class="table table-striped table-bordered table-condensed">
    <tr>
        <th class="text-right">ID</th>
        <th>Name</th>
        <th>Actions</th>
    </tr>
    @foreach (var item in Model)
    {
        <tr>
            <td class="text-right">@item.Id</td>
            <td>@item.Name</td>
            <td class="span2">
                <form asp-action="Remove" asp-controller="Category" method="post">
                    <a asp-action="Edit" asp-controller="Category" class="btn btn-sm btn-warning"
                       asp-route-categoryId="@item.Id">
                        Edit
                    </a>
                    <input type="hidden" name="CategoryId" value="@item.Id" />
                    <button type="submit" class="btn btn-danger btn-sm">
                        Delete
                    </button>
                </form>
            </td>
        </tr>
    }
</table>

<div class="text-center">
    <a asp-action="Create" asp-controller="Category" class="btn btn-primary">Add Category</a>
</div>